// Servicio para usuarios

export class UserService {
    static async getUserById(userId: string) {
        // return fetch(`${API_ENDPOINTS.USUARIOS.GET_BY_ID(userId)}`)
        throw new Error("No implementado");
    }
}
